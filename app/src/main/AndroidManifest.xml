<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pasian.efrei.colorfull">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Main_menu"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/wheely"></activity>
        <activity
            android:name=".ColorList"
            android:label="@string/title_color_list"
            android:theme="@style/AppTheme.NoActionBar" />
        <!--
        <activity
            android:name=".ColorDetailActivity"
            android:label="@string/title_color_detail"
            android:parentActivityName=".ColorListActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="pasian.efrei.colorfull.ColorListActivity" />
        </activity>
        <activity android:name=".ColorList">

        </activity>
        -->
        <activity android:name=".Schemer"></activity>
    </application>

</manifest>